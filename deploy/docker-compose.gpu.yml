services:
  tts-worker:
    build:
      context: ../services/tts-worker
      dockerfile: Dockerfile
    env_file: ../.env
    volumes:
      - tts-models:/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    depends_on:
      - redis

volumes:
  tts-models:
